name: NodeJS with Grunt + Playwright

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  e2e:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]  # Node >=20 para Angular 19

    steps:
    # 1️⃣ Checkout repo de tests
    - name: Checkout tests repo
      uses: actions/checkout@v4

    # 2️⃣ Clonar frontend
    - name: Clone frontend
      run: git clone https://github.com/jhonnierdlc/front-test.git front-test

    # 3️⃣ Clonar backend
    - name: Clone backend
      run: git clone https://github.com/jhonnierdlc/back-test.git back-test

    # 4️⃣ Instalar dependencias backend
    - name: Install backend dependencies
      run: npm install --legacy-peer-deps
      working-directory: ./back-test

    # 5️⃣ Levantar backend en segundo plano
    - name: Start backend
      run: npm run start &
      working-directory: ./back-test

    # 6️⃣ Instalar dependencias frontend
    - name: Install frontend dependencies
      run: npm install --legacy-peer-deps
      working-directory: ./front-test

    # 7️⃣ Build frontend
    - name: Build frontend
      run: npm run build
      working-directory: ./front-test

    # 8️⃣ Servir frontend de manera confiable
    - name: Serve frontend
      run: |
        npm install -g serve wait-on
        serve -s dist/employee-app -l 4200 --single &
        npx wait-on http://localhost:4200 --timeout 60000
      working-directory: ./front-test

    # 9️⃣ Instalar dependencias de tests (solo npm install, sin postinstall)
    - name: Install test dependencies
      run: npm install --legacy-peer-deps
      working-directory: .

    # 🔟 Instalar navegadores de Playwright explícitamente (evita Permission denied)
    - name: Install Playwright browsers
      run: npx playwright install --with-deps
      working-directory: .

    # 1️⃣1️⃣ Ejecutar Playwright mediante script de npm
    - name: Run Playwright tests
      run: npm run test:e2e
      working-directory: .

    # 1️⃣2️⃣ Subir reporte HTML
    - name: Upload Playwright report
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: playwright-report
        path: ./playwright-test/playwright-report/
